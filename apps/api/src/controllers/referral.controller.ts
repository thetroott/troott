// referral controller (invitations)

    // generate referral (with code and link)
    // share referral
    // delete referral
    // validate refferal code
    // get all refferal

    // referral status by userId
    // referral history by userId
    // get referral status (who referred the user)
    // get referral status (who used the referral code)

    // track activity (Logs sermon listens (could be event-based or manual API call))
    // referral progress (Returns progress toward referral success criteria)

    // get rewards (Return how many successful invites the user has)
    // grant reward (when user hits mark, Grants 3-month subscription and triggers a notification)
