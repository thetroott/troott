/**
 * Re-exports player mutation DTOs from `@/api/dtos`.
 * Prefer `@/api/dtos/player-mutations.dto` in new code.
 */
export type {
    AddToQueueMutation,
    QueueMutationDTO,
    QueueOrderMutation,
} from '@/api/dtos/player-mutations.dto';
