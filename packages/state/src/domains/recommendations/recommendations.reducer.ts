import type { RecommendationsAction, RecommendationsState } from './recommendations.types';

export function recommendationsReducer(
    state: RecommendationsState,
    _action: RecommendationsAction,
): RecommendationsState {
    return state;
}
