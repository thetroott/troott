import type { EngagementAction, EngagementState } from './engagement.types';

export function engagementReducer(
    state: EngagementState,
    _action: EngagementAction,
): EngagementState {
    return state;
}
