import type { DownloadsAction, DownloadsState } from './downloads.types';

export function downloadsReducer(
    state: DownloadsState,
    _action: DownloadsAction,
): DownloadsState {
    return state;
}
