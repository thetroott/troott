import type { OnboardingAction, OnboardingState } from './onboarding.types';

export function onboardingReducer(
    state: OnboardingState,
    _action: OnboardingAction,
): OnboardingState {
    return state;
}
