# feat-0001 — CI: lint, typecheck, test, build, expo-doctor
# Spec: specs/platform/feature/feat-0001/TECH.md

name: CI

on:
    push:
        branches:
            - development
            - staging
            - main
            - favour-development
            - damola-development
        paths-ignore:
            - '**.md'
            - 'docs/**'
            - 'specs/**'
    pull_request:
        paths-ignore:
            - '**.md'
            - 'docs/**'
            - 'specs/**'

concurrency:
    group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
    cancel-in-progress: true

env:
    NODE_VERSION: '22'
    PNPM_VERSION: '10.33.0'
    TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
    TURBO_TEAM: ${{ vars.TURBO_TEAM }}

jobs:
    validate:
        name: Validate
        runs-on: ubuntu-latest
        timeout-minutes: 20

        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                  fetch-depth: 2

            - name: Setup monorepo
              uses: ./.github/actions/setup-monorepo

            - name: Lint
              run: pnpm lint
              continue-on-error: true

            - name: Typecheck
              run: pnpm typecheck:workspace

            - name: Typecheck API
              run: pnpm typecheck:api
              continue-on-error: true

            - name: Format check
              run: pnpm format:check
              continue-on-error: true

    test:
        name: Test
        runs-on: ubuntu-latest
        timeout-minutes: 20
        needs: validate

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup monorepo
              uses: ./.github/actions/setup-monorepo

            - name: Run tests
              run: pnpm test:ci
              continue-on-error: true

    build:
        name: Build
        runs-on: ubuntu-latest
        timeout-minutes: 25
        needs: validate

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup monorepo
              uses: ./.github/actions/setup-monorepo

            - name: Setup Turborepo cache
              uses: turborepo/cache-action@v1
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}

            - name: Build deployable apps
              run: pnpm build:ci

            - name: Upload build artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: troott-build-artifacts
                  path: |
                      apps/api/dist
                      apps/web/dist
                      apps/website/.next
                  retention-days: 1
                  if-no-files-found: error

    mobile:
        name: Expo doctor
        runs-on: ubuntu-latest
        timeout-minutes: 15
        needs: validate

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup monorepo
              uses: ./.github/actions/setup-monorepo

            - name: Expo doctor
              run: pnpm dlx expo-doctor@latest
              working-directory: apps/mobile

    security:
        name: Security
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup monorepo
              uses: ./.github/actions/setup-monorepo

            - name: pnpm audit
              run: pnpm audit --prod
              continue-on-error: true

            - name: TruffleHog secret scan
              uses: trufflesecurity/trufflehog@main
              with:
                  path: ./
                  base: ${{ github.event.repository.default_branch }}
                  head: HEAD
              continue-on-error: true

    ci-success:
        name: CI success
        runs-on: ubuntu-latest
        needs: [validate, build, mobile]
        if: always()

        steps:
            - name: Check required jobs
              run: |
                  if [ "${{ needs.validate.result }}" != "success" ] || \
                     [ "${{ needs.build.result }}" != "success" ] || \
                     [ "${{ needs.mobile.result }}" != "success" ]; then
                    echo "CI pipeline failed"
                    exit 1
                  fi
                  echo "CI pipeline passed"
